<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bandas de <%= genero %></title>
</head>
<body>
    <h1>Bandas de <%= genero %></h1>
    <ul>
        <% bandas.forEach(function(banda) { %>
            <li>
                <strong><%= banda.nombre %></strong> 
                (<a href="/bandas/<%= banda.id %>">Ver detalles</a>)
            </li>
        <% }); %>
    </ul>
</body>
</html>